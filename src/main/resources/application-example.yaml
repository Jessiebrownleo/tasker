spring:
  application:
    name: tasker
  datasource:
    url: jdbc:postgresql://localhost:5432/tasker
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8080
app:
  security:
    jwt:
      secret: "change-me-please-change-me-please-32bytes-minimum"
      issuer: "tasker-app"
      access-token-ttl-min: 60
      refresh-token-ttl-days: 7
